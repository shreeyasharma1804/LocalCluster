apiVersion: kustomize.toolkit.fluxcd.io/v1
kind: Kustomization
metadata:
  name: cluster-repo-kustomize
  namespace: flux-system
spec:
  interval: 10m0s
  path: /
  prune: true
  sourceRef:
    kind: GitRepository
    name: cluster-repo
